import { FirstlettervalidatorDirective } from './firstlettervalidator.directive';

describe('FirstlettervalidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new FirstlettervalidatorDirective();
    expect(directive).toBeTruthy();
  });
});
